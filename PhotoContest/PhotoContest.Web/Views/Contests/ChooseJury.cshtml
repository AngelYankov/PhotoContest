@model InviteViewModel

@{
    ViewData["Title"] = "ChooseJury";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Choose jury for contest</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ChooseJury">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Name" />
            <div class="form-group">
                <label asp-for="Username" class="control-label">User to choose:</label>
                <select asp-for="Username" class="form-control" asp-items="ViewBag.Users"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Choose" class="btn btn-success" />
                <a class="btn btn-dark" asp-action="Index">Back</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}